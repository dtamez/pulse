apiVersion: apps/v1
kind: Deployment
metadata:
  name: pulse-redis
  namespace: pulse
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pulse-redis
  template:
    metadata:
      labels:
        app: pulse-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
